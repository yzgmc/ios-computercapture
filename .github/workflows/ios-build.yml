name: iOS Build

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-15
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Select Xcode version
        run: sudo xcode-select -s /Applications/Xcode_16.app/Contents/Developer

      - name: Show Xcode version
        run: xcodebuild -version

      - name: Show available simulators
        run: xcrun simctl list devices

      - name: Install iOS platform
        run: xcodebuild -downloadPlatform iOS

      - name: Install xcodegen
        run: brew install xcodegen

      - name: Generate Xcode project
        working-directory: ios
        run: xcodegen generate

      - name: Inspect generated project
        working-directory: ios
        run: |
          ls -la
          ls -la PhoneCam.xcodeproj/xcshareddata/xcschemes/ || true

      - name: Install CocoaPods dependencies
        working-directory: ios
        run: |
          gem install cocoapods
          pod install --repo-update

      - name: Inspect workspace schemes
        working-directory: ios
        run: |
          xcodebuild -workspace PhoneCam.xcworkspace -list

      - name: Build iOS app
        working-directory: ios
        run: |
          set -o pipefail
          xcodebuild clean build \
            -workspace PhoneCam.xcworkspace \
            -scheme PhoneCam \
            -sdk iphonesimulator \
            -destination 'platform=iOS Simulator,name=iPhone 15' \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO \
            | xcbeautify --renderer github-actions

      - name: Archive iOS app
        working-directory: ios
        run: |
          set -o pipefail
          xcodebuild archive \
            -workspace PhoneCam.xcworkspace \
            -scheme PhoneCam \
            -sdk iphoneos \
            -archivePath $RUNNER_TEMP/PhoneCam.xcarchive \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGN_IDENTITY="" \
            | xcbeautify --renderer github-actions
        continue-on-error: true

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: ios-build-artifacts
          path: |
            ios/build/**
            $RUNNER_TEMP/PhoneCam.xcarchive
          if-no-files-found: ignore
